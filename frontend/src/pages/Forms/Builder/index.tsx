import React from 'react';

const FormBuilder: React.FC = () => {
    return (
        <div style={{ padding: 24 }}>
            <h2>Form Builder</h2>
            <p>Kéo thả / Add field (type, label, required)</p>
            <p>Lưu form lại thành JSON</p>
        </div>
    );
};

export default FormBuilder;
