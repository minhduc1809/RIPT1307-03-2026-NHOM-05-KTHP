import React from 'react';

const WorkflowBuilder: React.FC = () => {
    return (
        <div style={{ padding: 24 }}>
            <h2>Workflow Builder</h2>
            <p>Add step</p>
            <p>Select approver role, add conditions...</p>
        </div>
    );
};

export default WorkflowBuilder;
