import React from 'react';

const Register: React.FC = () => {
    return (
        <div style={{ padding: 24, textAlign: 'center' }}>
            <h2>Đăng Ký</h2>
            <p>Form đăng ký (Name, Email, Password)</p>
        </div>
    );
};

export default Register;
